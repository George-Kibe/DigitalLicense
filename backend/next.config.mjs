/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "qddl-images-bucket.s3.amazonaws.com"
    ]
  }
};
export default nextConfig;
